{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/os-beta/:path*", "destination": "/api/os-beta" },
    { "source": "/:path*", "destination": "/index.html" }
  ]
}
